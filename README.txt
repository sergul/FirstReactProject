Environment Setup

1. Install VSCode

2. Install Node.js from https://nodejs.org/en/ (check with "node -v" command). NOTE: npm comes with Node.js installation

3. Install yarn by "npm install -g yarn" (check with "yarn --version")