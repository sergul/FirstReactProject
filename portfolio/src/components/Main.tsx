import React, { PureComponent } from 'react';
import Header from './Header';

class Main extends PureComponent {
  render() {
    return (
      <div>
        Main page
      </div>
     
    );
  }
}

export default Main;